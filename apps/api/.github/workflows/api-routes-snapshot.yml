name: API Routes Snapshot

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  detect-route-changes:
    name: Detect API Route Changes
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch all history for comparison

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run build

      - name: Download previous OpenAPI spec (if exists)
        id: download-previous
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: openapi-spec
          path: .

      - name: Generate OpenAPI specification
        id: generate-spec
        run: |
          echo "🔧 Gerando especificação OpenAPI..."
          npm run openapi:generate
          
          if [ ! -f "openapi-spec.json" ]; then
            echo "❌ Erro: openapi-spec.json não foi gerado"
            exit 1
          fi
          
          echo "✅ OpenAPI spec gerada com sucesso"
          ls -lh openapi-spec.json
          
          # Se não existe spec anterior, copia a atual como previous para próxima execução
          if [ ! -f "openapi-spec.previous.json" ]; then
            echo "📝 Primeira execução - copiando spec atual como previous"
            cp openapi-spec.json openapi-spec.previous.json
          else
            echo "✅ Spec anterior encontrada para comparação"
          fi

      - name: Detect route changes
        id: detect-changes
        run: |
          if [ ! -f "openapi-spec.json" ]; then
            echo "❌ Erro: openapi-spec.json não encontrado"
            exit 1
          fi
          
          echo "🔍 Detectando mudanças nas rotas..."
          npm run openapi:changes
          
          if [ -f "src/docs/route-changes-report.md" ]; then
            echo "✅ Relatório de mudanças gerado"
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "⚠️ Relatório não foi gerado"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi
        continue-on-error: true

      - name: Generate summary
        run: |
          echo "## 📊 Mudanças nas Rotas da API" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ -f "src/docs/route-changes-report.md" ]; then
            # Extrair resumo do relatório
            if grep -q "Nenhuma mudança detectada" src/docs/route-changes-report.md; then
              echo "✅ **Nenhuma mudança detectada nas rotas.**" >> $GITHUB_STEP_SUMMARY
            else
              # Extrair seções do relatório
              echo "### Resumo" >> $GITHUB_STEP_SUMMARY
              grep -A 4 "## 📈 Resumo" src/docs/route-changes-report.md | tail -n +2 >> $GITHUB_STEP_SUMMARY || true
              echo "" >> $GITHUB_STEP_SUMMARY

              # Mostrar rotas adicionadas
              if grep -q "## ➕ Rotas Adicionadas" src/docs/route-changes-report.md; then
                echo "### ➕ Rotas Adicionadas" >> $GITHUB_STEP_SUMMARY
                grep -A 1 "### \`" src/docs/route-changes-report.md | grep -E "^\`" | head -10 >> $GITHUB_STEP_SUMMARY || true
                echo "" >> $GITHUB_STEP_SUMMARY
              fi

              # Mostrar rotas removidas
              if grep -q "## ➖ Rotas Removidas" src/docs/route-changes-report.md; then
                echo "### ➖ Rotas Removidas" >> $GITHUB_STEP_SUMMARY
                grep -A 1 "### \`" src/docs/route-changes-report.md | grep -E "^\`" | head -10 >> $GITHUB_STEP_SUMMARY || true
                echo "" >> $GITHUB_STEP_SUMMARY
              fi

              # Mostrar rotas modificadas
              if grep -q "## 🔄 Rotas Modificadas" src/docs/route-changes-report.md; then
                echo "### 🔄 Rotas Modificadas" >> $GITHUB_STEP_SUMMARY
                grep -A 1 "### \`" src/docs/route-changes-report.md | grep -E "^\`" | head -10 >> $GITHUB_STEP_SUMMARY || true
                echo "" >> $GITHUB_STEP_SUMMARY
              fi

              echo "📄 **Relatório completo disponível nos artifacts**" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "⚠️ Não foi possível gerar o relatório de mudanças." >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload OpenAPI specification
        uses: actions/upload-artifact@v4
        if: steps.generate-spec.outcome == 'success'
        continue-on-error: true
        with:
          name: openapi-spec
          path: openapi-spec.previous.json
          retention-days: 30

      - name: Upload route changes report
        uses: actions/upload-artifact@v4
        if: steps.detect-changes.outcome == 'success' && steps.detect-changes.outputs.has_changes == 'true'
        continue-on-error: true
        with:
          name: route-changes-report
          path: src/docs/route-changes-report.md
          retention-days: 30

      - name: Comment PR with route changes
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');

            try {
              const reportPath = path.join(process.cwd(), 'src', 'docs', 'route-changes-report.md');
              
              if (fs.existsSync(reportPath)) {
                const report = fs.readFileSync(reportPath, 'utf-8');
                
                // Verificar se há mudanças
                if (!report.includes('Nenhuma mudança detectada')) {
                  const body = `## 📊 Mudanças nas Rotas da API\n\n${report}\n\n---\n\n*Este relatório foi gerado automaticamente pela GitHub Action.*`;
                  
                  github.rest.issues.createComment({
                    issue_number: context.issue.number,
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    body: body
                  });
                }
              }
            } catch (error) {
              console.log('Não foi possível comentar no PR:', error.message);
            }

