/**
 * Re-exporta tipos do Fastify para garantir que sejam carregados
 * Este arquivo é necessário para que o TypeScript encontre as declarações de módulo
 */
export {};

// As declarações de módulo em fastify.d.ts serão automaticamente carregadas pelo TypeScript
